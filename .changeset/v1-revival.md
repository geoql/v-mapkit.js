---
'@geoql/v-mapkit.js': major
---

Rewrite as a fully declarative Vue 3 component library: real `<VMap>` + annotation/overlay components using provide/inject, multi-map support, real event emits, proper lifecycle cleanup. Modernized toolchain (Vite 7, oxlint + oxfmt, TS 5, vue-tsc 3, husky 9). **Breaking:** the old function-based annotation/overlay API and single-map global state are removed.
